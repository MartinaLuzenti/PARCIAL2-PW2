 function UserGrid() {
  return (
    <div className="user-grid">
    
     
      Esta es una lista de usuarios
    </div>
  );
}
export default UserGrid;
// This component renders a grid of user cards. Each card displays the user's avatar, name, and email.